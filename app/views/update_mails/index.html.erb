<div class="row animated fadeIn" style="margin-top: 20px">
    <div class="col-lg-12">
        <div class="ibox float-e-margins main-box">
            <div class="ibox-title">
                <h5>Update Mails</h5>
            </div>
            <div class="ibox-content">
                <div class="table-responsive">
                    <%= form_tag update_mails_path, method: 'get', id: 'search-field' do %>
                        <div class="col-sm-4" style="padding-left: 0;padding-bottom: 5px">
                            <%= text_field_tag :search, params[:search],
                                               class: 'form-control input-sm pull-left',
                                               placeholder: 'Search for update mails' %>
                        </div>
                    <% end %>
                    <%= link_to({:controller => 'update_mails', :action => 'new'},
                                {:class => 'btn btn-sm btn-primary pull-right', :style => 'color: white'}, {}) do %>
                        <i class="fa fa-plus" style="margin-right: 5px"></i><span style="">New Update Mail</span>
                    <% end %>
                    <div id="data-table">
                        <%= render 'update_mails' %>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>